@model CameraControlTagVM

<div class="card shadow border-0 my-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
    <div class="row">
        <div class="col-12 text-center">
                <h2 class="text-white py-2"> @(Model.CameraControlTag.Id!=0?"Update":"Create") CameraControlTag</h2>
        </div>
    </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row" enctype="multipart/form-data">
            <input asp-for="CameraControlTag.Id" hidden />
           
            <div class="row">
                <div class="col-10">
                    <div class="border p-3">
                        @*<div asp-validation-summary="ModelOnly"></div>*@
                        <div class="form-floating py-2 col-12">
                            <input asp-for="CameraControlTag.TagName" class="form-control border-0 shadow" />
                            <label asp-for="CameraControlTag.TagName" class="ms-2"></label>
                            <span asp-validation-for="CameraControlTag.TagName" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <label asp-for="CameraControlTag.Description" class="ms-2 text-muted"></label>
                            <textarea asp-for="CameraControlTag.Description" class="form-control border-0 shadow"></textarea>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <label asp-for="CameraControlTag.CreatedTs" class="ms-2 text-muted"></label>
                            <textarea asp-for="CameraControlTag.CreatedTs" class="form-control border-0 shadow"></textarea>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <label asp-for="CameraControlTag.UpdatedTs" class="ms-2 text-muted"></label>
                            <textarea asp-for="CameraControlTag.UpdatedTs" class="form-control border-0 shadow"></textarea>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <select asp-for="@Model.CameraControlTag.CameraId" asp-items="@Model.CameraList" class="form-select border-0 shadow">
                                <option disabled selected>--Select Camera--</option>
                            </select>
                            <label asp-for="CameraControlTag.CameraId" class="ms-2"></label>
                            <span asp-validation-for="CameraControlTag.CameraId" class="text-danger"></span>
                        </div>

                        <div class="row pt-2">
                            <div class="col-6 col-md-3">
                                @if (Model.CameraControlTag.Id != 0)
                                {
                                    <button type="submit" class="btn btn-primary form-control">Update</button>
                                }
                                else
                                {
                                    <button type="submit" class="btn btn-primary form-control">Create</button>
                                }
                            </div>
                            <div class="col-6 col-md-3">
                                <a asp-controller="CameraControlTag" asp-action="Index" class="btn btn-outline-primary border  form-control">
                                    Back to List
                                </a>
                            </div>
                        </div>


                    </div>
                </div>
            </div>
           
        </form>
    </div>
</div>




@section Scripts{
@*     <script>
        tinymce.init({
            selector: 'textarea',
            plugins: 'anchor autolink charmap codesample emoticons  lists   table visualblocks wordcount',
            toolbar: 'undo redo | blocks fontfamily fontsize | bold italic underline strikethrough |  align lineheight | numlist bullist indent outdent',
        });
    </script> *@
    @{
    <partial name="_ValidationScriptsPartial"/>
    }
}