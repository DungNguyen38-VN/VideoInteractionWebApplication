@{
    ViewData["Title"] = "Home Page";
}

<div class="hero-section d-flex justify-content-center align-items-center text-center text-white">
    <div class="overlay"></div>
    <div class="content">
        <h1 class="display-3">Welcome to Video Interaction</h1>
        <p class="lead">Experience seamless video communication with our cutting-edge platform.</p>
       @*  @if (User.IsInRole(SD.Role_Customer) || User.IsInRole(SD.Role_Admin))
        {
           
        }
        else
        {
            <a href="/Identity/Account/Login" class="btn btn-lg btn-primary mt-4">Login</a>
        } *@

    </div>
</div>

<section class="features-section py-5">
    <div class="container text-center">
        <h2 class="mb-4">Why Choose Us?</h2>
        <div class="row">
            <div class="col-md-4">
                <i class="bi bi-camera-video display-4 text-primary"></i>
                <h4 class="mt-3">High-Quality Video</h4>
                <p>Crystal clear video and audio for the best interaction experience.</p>
            </div>
            <div class="col-md-4">
                <i class="bi bi-shield-lock display-4 text-primary"></i>
                <h4 class="mt-3">Secure Platform</h4>
                <p>We prioritize your privacy with top-notch security measures.</p>
            </div>
            <div class="col-md-4">
                <i class="bi bi-speedometer2 display-4 text-primary"></i>
                <h4 class="mt-3">Fast & Reliable</h4>
                <p>Optimized for speed to ensure smooth and uninterrupted sessions.</p>
            </div>
        </div>
    </div>
</section>
<style>
    .hero-section {
        position: relative;
        background-image: url('~/images/homescreen.jpg');
        background-size: cover;
        background-position: center;
        height: 50vh;
        color: white;
    }

        .hero-section .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
        }

        .hero-section .content {
            position: relative;
            z-index: 1;
        }

    .features-section {
        background-color: #f8f9fa;
    }

</style>